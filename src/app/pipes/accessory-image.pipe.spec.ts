import { AccessoryImagePipe } from './accessory-image.pipe';

describe('AccessoryImagePipe', () => {
  it('create an instance', () => {
    const pipe = new AccessoryImagePipe();
    expect(pipe).toBeTruthy();
  });
});
