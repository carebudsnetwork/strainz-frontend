import { AccessoryTypePipe } from './accessory-type.pipe';

describe('AccessoryTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AccessoryTypePipe();
    expect(pipe).toBeTruthy();
  });
});
